<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".activity.ChangePhotoActivity">

    <ImageView
        android:id="@+id/image_change_photo"
        android:layout_width="140dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="15dp"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/text_change_photo_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_change_photo"
        android:layout_toStartOf="@id/text_change_photo_confirm"
        android:layout_marginEnd="20dp"
        android:text="@string/cancel"
        android:textSize="16sp"
        android:textColor="@color/colorAccent" />

    <TextView
        android:id="@+id/text_change_photo_confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_change_photo"
        android:layout_alignParentEnd="true"
        android:text="@string/confirm"
        android:textSize="16sp"
        android:textColor="@color/colorAccent"/>

</RelativeLayout>